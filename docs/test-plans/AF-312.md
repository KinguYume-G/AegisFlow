# Test Plan — AF-312 Gate 2 evidence publication

**Status:** Approved v1 — Project Owner approved 2026-08-03

## Scope

Validate that the Gate 2 report, blog, and demo runbook are complete, reproducible, Secret-safe, internally consistent, and limited to evidence that actually exists.

## Unit Tests

Not applicable: AF-312 changes no application code.

## Component Tests

- Verify all repository-relative Markdown links and anchors.
- Verify every reported metric exactly matches its cited hosted run.
- Verify MANIFEST counts and paths match the filesystem.
- Verify traceability includes AF-308 through AF-312.

## Integration Tests

- Confirm PR #100 is merged at `5a5b6a33e4cdf0750a5e3f2d86ddf0766c06d016` and required CI succeeded.
- Confirm Gate 2 fault run `30801262477` completed successfully on that merge commit.
- Confirm artifact `gate2-fault-evidence-30801262477` / ID `8850930348` exists and is not expired at review time.
- Confirm model smoke run `30804037539` completed successfully on the same merge commit.
- Confirm its redacted result reports primary `deepseek/deepseek-v4-flash`, resolved model `deepseek-v4-flash`, measured tokens, provider-reported cost, and no fallback attempt.

## Negative / Security Tests

- Scan the complete diff for private keys, provider tokens, GitHub tokens, API keys, `.env` values, and raw model response content.
- Ensure documents never claim that Gemini fallback was exercised by the real smoke.
- Ensure deterministic fallback results are clearly labeled as tests rather than live-provider evidence.
- Ensure no workflow dispatch, deployment, Issue closure, or Gate acceptance occurs merely from rendering the report.

## Fault Injection

No new fault campaign is required. Cite the successful hosted 4×5 campaign:

- iterations: 20;
- accepted: true;
- duplicate external effects: 0;
- lost Signals: 0;
- p50: 2049.31 ms;
- p95: 2973.56 ms.

## Fixtures

- GitHub Actions run metadata and redacted logs for run IDs `30801262477` and `30804037539`.
- Existing deterministic Saga, circuit-breaker, gateway-fallback, PostgreSQL, and Temporal tests from PR #100.

## Expected Results

- Three publishable Gate 2 documents exist.
- All claims map to immutable evidence or are explicitly marked as limitations.
- Link, inventory, traceability, and Secret checks have zero failures.
- No source, migration, dependency, workflow, architecture, or ADR file changes.
- PR stops for Human Review; AF-312 and M3 are not marked complete before merge.

## Commands

- Markdown relative-link and anchor validator.
- MANIFEST-to-filesystem reconciliation.
- `git diff --check` and exact changed-path audit.
- Secret-pattern scan over the staged diff.
- Read-only `gh pr view`, `gh run view`, and artifact metadata queries.

## Evidence

The PR body records commands, counts, immutable GitHub URLs, changed-file scope, and the final reviewer decision.

## Limitations

- The fault environment is an ephemeral GitHub-hosted runner with a single Temporal service, not a multi-region production cluster.
- The model smoke contains one real primary-route request; no real DeepSeek outage or Gemini fallback occurred.
- Cost and token fields are provider/SDK evidence, not billing reconciliation.
- Load, chaos duration, OIDC/RBAC, multi-tenant security, and deployment evidence belong to later milestones.
